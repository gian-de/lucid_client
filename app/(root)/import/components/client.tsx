import { ImportVehicle, columns } from "@/app/(root)/import/components/columns";

type Props = {
  data: ImportVehicle[];
};

const ImportClient = (data: Props) => {
  return <div>testing</div>;
};

export default ImportClient;
